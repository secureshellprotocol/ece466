#ifndef __PARSER_ENUM_JR
#define __PARSER_ENUM_JR
enum tokens {
    // ast node types
    UNAOP=400,
    BINOP,
    TERNOP,
    TYPECAST,
    FUNCTION,
    LIST,
    POINTER,
    ARRAY,
    VARIABLE
};
#endif
